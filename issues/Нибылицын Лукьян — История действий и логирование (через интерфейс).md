# Нибылицын Лукьян (SuperLuchito) — История действий и логирование: аудит и экспорт

## Контекст

- **Раздел:** `/admin/monitoring`
- **Роли:** Администратор / частично Нотариус (только свои заказы)
- **Связан с:** Черненко Дмитрий (просмотр логов по пользователю/заказу и события безопасности)
- **Связанные файлы:** `libs/web/admin/src/lib/`

---

## Задача

Реализовать главную страницу мониторинга: общая лента аудит-событий с фильтрами по типу/дате/действию, экспорт лога.

---

## Маршрут

| Маршрут             | Компонент             | Описание                        |
| ------------------- | --------------------- | ------------------------------- |
| `/admin/monitoring` | `MonitoringComponent` | Аудит-лента + фильтры + экспорт |

---

## Генерация компонентов

```bash
pnpm nx g @nx/angular:component libs/web/admin/src/lib/features/monitoring/monitoring/monitoring --standalone
pnpm nx g @nx/angular:component libs/web/admin/src/lib/features/monitoring/audit-log-item/audit-log-item --standalone
pnpm nx g @nx/angular:component libs/web/admin/src/lib/features/monitoring/audit-filters/audit-filters --standalone
```

---

## API-контракты (`@notary-portal/api-contracts`)

> **Важно:** backend/API интеграция audit вынесена в отдельную задачу D10 и не входит в текущую frontend-итерацию.  
> В репозитории сейчас нет `audit.proto`, нет `libs/api/audit`, и нет регистрации сервиса в `apps/api/src/app/connect-router.registry.ts`.  
> Использовать **mock-данные** с полным TODO-комментарием. Mock должен точно отражать ожидаемую структуру реального ответа.

```typescript
// TODO(D10): заменить mock на реальный вызов после появления audit.proto,
// libs/api/audit и регистрации сервиса в ConnectRouterRegistry
// AuditLogService.listAuditLogs({ filters: { userId?, entityId?, action?, dateFrom?, dateTo? }, pagination })
// → { logs: AuditLog[], meta: PaginationMeta }
```

---

## Mock-структура события

```typescript
export interface AuditLog {
  id: string;
  actorId: string; // кто совершил действие
  actorEmail: string;
  actorRole: string;
  action: string; // 'assessment.created' | 'user.blocked' | 'payment.completed' | ...
  entityType: string; // 'Assessment' | 'User' | 'Payment' | ...
  entityId: string;
  description: string; // «Заявка #123 создана»
  ipAddress: string;
  createdAt: string; // ISO-datetime
  severity: 'info' | 'warning' | 'error';
}
```

Mock-данные: 50 записей в `audit-logs.mock.ts` с разными типами событий.

---

## Ключевые функции

### Страница мониторинга (`/admin/monitoring`)

- Заголовок с кнопкой **«Экспорт CSV»**
- Ссылки-вкладки: «Все события» (Лукьян) | «По пользователям» (Черненко) | «События безопасности» (Черненко)

### `AuditFiltersComponent`

- Фильтры:
  - Тип действия — select (из уникальных `action` в mock-данных)
  - Тип сущности — select (из уникальных `entityType`)
  - Severity — select: «Все», «Info», «Warning», «Error»
  - Дата «от» / «до» — два `input[date]`
  - actorEmail — поле поиска
- Кнопка «Сбросить»
- `@Output() filtersChanged: EventEmitter<AuditFilters>`

### Лента событий

- Список `AuditLogItemComponent`:
  - Цветная иконка по severity (info — синий, warning — жёлтый, error — красный)
  - `createdAt` — «ДД.ММ.ГГГГ ЧЧ:ММ:СС»
  - actorEmail + actorRole
  - Описание действия
  - entityType + entityId
  - IP-адрес (серый)
- Клик по строке → открывает детальную панель (inline expand или боковая)
- Пагинация (клиентская по mock-данным)

### Экспорт CSV

```typescript
const csv = filteredLogs
  .map((l) =>
    [l.createdAt, l.actorEmail, l.action, l.entityType, l.entityId, l.ipAddress, l.severity].join(
      ',',
    ),
  )
  .join('\n');
```

---

## Технические требования

- Вся фильтрация — клиентская по mock-массиву
- `computed()` для отфильтрованного списка на основе `signal`-фильтров
- Обновить `admin.routes.ts`

---

## Критерии готовности

- [ ] Лента событий с 50 mock-записями отображается
- [ ] Все фильтры работают (клиентская фильтрация)
- [ ] Сброс фильтров
- [ ] Экспорт CSV работает
- [ ] Пагинация по отфильтрованным данным
- [ ] Детальная панель при клике на событие
